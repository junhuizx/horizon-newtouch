import requests
import json


from keystoneclient.auth.identity import v2
from keystoneclient import session
from novaclient import client


url = "http://localhost/dashboard/newtouch/api/v1/server/"


#re = sess.get(url, headers=headers2)
#re = requests.get(url, headers=headers2)

#print re.text

#object = json.loads(re.text)
#print object

